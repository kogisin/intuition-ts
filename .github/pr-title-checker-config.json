{
  "LABEL": {
    "name": "title needs formatting",
    "color": "CDAC11"
  },
  "CHECKS": {
    "regexp": "^([A-Z]{2,}-[0-9]+|[A-Z]{2,}-[0-9]+( )[A-Z]{2,}-[0-9]+) ((feat|fix|chore)[(](portal|1ui|api|core|protocol|sdk|tools)[)]|chore): ([a-zA-Z 0-9 - & ,]+( [a-zA-Z 0-9 -]+)+)",
    "regexpFlags": "i"
  },
  "MESSAGES": {
    "success": "All good",
    "failure": "Doesn't match required format",
    "notice": ""
  }
}
