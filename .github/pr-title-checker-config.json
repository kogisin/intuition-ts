{
  "LABEL": {
    "name": "title needs formatting",
    "color": "CDAC11"
  },
  "CHECKS": {
    "regexp": "^([A-Z]{3,}-[0-9]+|[A-Z]{3,}-[0-9]+( )[A-Z]{3,}-[0-9]+) ((feat|fix|chore)[(][(portal|1ui|core|subgraph|protocol|sdk|substream|api|tools),]+[)]): ([a-zA-Z 0-9 - & ,]+( [a-zA-Z 0-9 -]+)+)",
    "regexpFlags": "i"
  },
  "MESSAGES": {
    "success": "All good",
    "failure": "Doesn't match required format",
    "notice": ""
  }
}
