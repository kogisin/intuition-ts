{
  "extends": "../../tsconfig.base.json", // Adjust the path as necessary
  "compilerOptions": {
    "baseUrl": "./src", // Overrides the base to set the root directory for relative module resolution to the src folder
    "paths": {
      "@/*": ["*"]
    }
  },
  "include": ["src"]
}
