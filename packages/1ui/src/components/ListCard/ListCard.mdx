import { Canvas, Controls, Meta, Source, Title } from '@storybook/blocks'

import * as Stories from './ListCard.stories'

<Meta of={Stories} />

<Title />

#### A card component for displaying list information with an image, title, identity count, and a wrappable view button.

<Source
  dark
  language="tsx"
  code={`
import { ListCard } from '@0xintuition/1ui'
import { Link } from 'your-router-library'

<ListCard
  displayName="My Favorite Claims"
  imgSrc="https://example.com/image.jpg"
  identitiesCount={42}
  buttonWrapper={(button) => <Link to="/lists/my-favorites">{button}</Link>}
/>
`} />

<Canvas of={Stories.BasicUsage} sourceState="none" />

## Properties

<Controls />

## Variants

### Without Image

<Canvas of={Stories.NoImage} sourceState="none" />
