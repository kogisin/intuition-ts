import { Canvas, Controls, Meta, Source, Title } from '@storybook/blocks'

import { Button } from '..'

import * as Stories from './Input.stories'

<Meta of={Stories} />

<Title />

#### Displays a form input field or a component that looks like an input field.

<Source
  dark
  language="tsx"
  code={`
import { Input } from '@0xintuition/1ui'

<Input type="email" placeholder="Email" />
`} />

<Canvas of={Stories.BasicUsage} sourceState="none" />

## Properties

<Controls />

## Variants

### Start Adornment (Icon)

<Source
  dark
  language="tsx"
  code={`
import { Input } from '@0xintuition/1ui'

<Input type="text" placeholder="Search" startAdornment="magnifying-glass" />
`} />

<Canvas of={Stories.StartAdornmentIcon} sourceState="none" />

### Start Adornment (Text)

<Source
  dark
  language="tsx"
  code={`
import { Input } from '@0xintuition/1ui'

<Input type="text" placeholder="Search" startAdornment="http://" />
`} />

<Canvas of={Stories.StartAdornmentLabel} sourceState="none" />

### End Adornment (Icon)

<Source
  dark
  language="tsx"
  code={`
import { Input } from '@0xintuition/1ui'

<Input type="number" placeholder="Enter amount..." endAdornment="ethereum" />
`} />

<Canvas of={Stories.EndAdornmentIcon} sourceState="none" />

### Start Adornment (Text)

<Source
  dark
  language="tsx"
  code={`
import { Input } from '@0xintuition/1ui'

<Input type="text" placeholder="Search" endAdornment=".com" />
`} />

<Canvas of={Stories.EndAdornmentLabel} sourceState="none" />
