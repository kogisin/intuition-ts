import { Canvas, Controls, Meta, Source, Title } from '@storybook/blocks'

import { Button } from '..'

import * as Stories from './ClaimStatus.stories'

<Meta of={Stories} />

<Title />

## A component showing the status of the positions on a Claim

<Source
  dark
  language="tsx"
  code={`
import { ClaimStatus, Claim } from './ClaimStatus'

<ClaimStatus claimsFor={10} claimsAgainst={5}>
  <Claim
    size="sm"
    subject={{label: '0xintuition' }}
    predicate={{ label: 'is really' }}
    object={{ label: 'cool' }}

/>

</ClaimStatus>
`} />

<Canvas of={Stories.BasicUsage} sourceState="none" />
